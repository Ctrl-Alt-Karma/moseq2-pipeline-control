# Decisions

Decisions D-001 through D-018 are dated 2026-07-29. D-019 is dated
2026-07-30. D-020 is dated 2026-07-31. D-021 through D-029 are dated
2026-08-13.

| ID | Decision | Rationale |
|---|---|---|
| D-001 | One active builder at a time. | Avoid overlapping implementation and ambiguous authorship. |
| D-002 | Fable is verifier, not builder. | Preserve an independent adversarial review channel. |
| D-003 | Claude Code is a backup builder or bounded verifier. | Use it only under an explicit, limited handoff. |
| D-004 | No SEEP integration during this repair phase. | The shared-evidence experiment is unrelated to the present control system. |
| D-005 | Study design and biological analysis are separate downstream workstreams. | Structural repair must not inherit experiment-specific assumptions. |
| D-006 | Raw recordings never go in implementation repositories. | Keep large/sensitive operational inputs external and separately governed. |
| D-007 | Unstamped provenance means unknown. | Missing provenance does not justify compatibility claims. |
| D-008 | Old superseded PRs and branches remain for audit history. | Preserve the record of prior proposals and why they were replaced. |
| D-009 | No merges without AJ. | AJ is the final merge authority. |
| D-010 | App flip correction and extraction-time flip correction are alternative workflows, not cumulative passes. | Layering them adds pi twice and can leave extraction flip metadata inconsistent with corrected frames and angles. |
| D-011 | An uncleared app journal sentinel remains authoritative and fail-closed. | Slot contents are useful diagnostics, but without a power-loss durability guarantee they are not sufficient for automatic recovery. |
| D-012 | Katya's exact existing Python 3.7 / NumPy 1.18.3 MoSeq Conda environment is the supported production environment for this study. | It is the environment that produced the study's existing work and is the only approved target for the structural real-data pilot. Preserve it; do not upgrade it in place. |
| D-013 | Python 3.8/3.11 migration, NumPy upgrades, modernization, and multi-environment equivalence testing are out of scope. | The study has frozen its production target. These subjects are not planned work in this control repository. |
| D-014 | Legacy preservation precedes real-data processing. | Export the WSL distribution, freeze environment and dependency custody, and verify locked source before an explicitly approved bounded pilot. |
| D-015 | The home WSL environment is the golden reference and pilot host, not an assumed final-analysis machine. | Full analysis may move to separate hardware, but the study environment identity may not drift with the hardware. |
| D-016 | The frozen environment is portable only through a complete locked offline deployment bundle or a SHA-256-verified import of the golden WSL archive. | Approximate version matches, floating references, and opportunistic reinstallations cannot establish equivalence. |
| D-017 | Every new machine must independently pass exact preflight and the versioned known-answer fixture before real data. | Installation success is not qualification. Any `UNRESOLVED` or `MISMATCH` result is fail-closed. |
| D-018 | `deployment/run_pipeline_guarded.sh` is the supported production entry point on a separate analysis machine. | It refuses unqualified execution and records the verified fingerprint and qualification binding in each analysis output. |
| D-019 | One explicit packet-created validation root is reused across all home-pilot phases. | Script 01 alone initializes a versioned root. Every later phase validates the marker, required prior-phase receipt, and absence of its own outputs before adding new child state. Arbitrary roots, partial runs, reruns, and unknown state fail closed. |
| D-020 | Phase 0 compares installed MoSeq source against three immutable code states and freezes the approved primary-project custody inputs. | `VANILLA` is the exact upstream `dattalab` release HEAD at the verified fork branch point; `FORK_RELEASE` is the exact `Ctrl-Alt-Karma` release SHA; `CANDIDATE` is the locked candidate SHA. The approved project, two explicit configuration files, and canonical classifier are recorded in `environment/LEGACY_PRODUCTION_TARGET.md`. The current primary config is the golden artifact; its extraction-time historical version remains explicitly unresolved rather than reconstructed. |
| D-021 | Final production kappa is `464159`; the generic calibration and Phase A/B gates are closed. | Both named seeds independently resolved to the same qualifying interior winner under the frozen R3 protocol; Architect V4 selected the kappa after independent Fable verification. |
| D-022 | The production model artifact is the seed-`20260802`, kappa-`464159`, 500-iteration final model, SHA-256 `5e10803af7017bd32cc491483fcfa3bfc570e617d427649b4d0f1ca86c49d964`. | No earlier pre-result artifact-level production binding was found. The predeclared Architect fallback selects the first-seed primary lineage rather than the replication seed. Downstream results were not inspected. |
| D-023 | The authoritative full Phase B verifier transport SHA-256 is `48a3e69baaaeaf91d70eff682bdf55b4ebc54e5414b2b2001db9e86bb1dce8b1`. | A previous textual transfer record was clerically truncated to 63 hex characters; the 64-character digest was re-hashed from the sealed ZIP. |
| D-024 | Real-session production validation is the next scientific gate; Protocol V4 R1 remains an Architect candidate until eight pre-result bindings are frozen. | Validation must not be tuned or selected after result-bearing output is visible. |
| D-025 | Generic calibration will not be reopened with Seed C, K300, another kappa, a 1000-iteration extension, a likelihood tie-break, or visualization selection absent contradictory primary evidence. | Those proposals are outside the closed protocol and do not resolve the current validation question. |
| D-026 | A reviewer expected to characterize frozen executable semantics should receive the executable rule, authoritative protocol, and conformance receipt when practical. | Phase A showed that numerical reproducibility alone did not prevent an incorrect prose characterization of the frozen rule. |
| D-027 | Transport construction and verification must enforce exact set equality: manifest members equal physical transport members union explicit exclusion-ledger members. | Phase B's six omitted manifested pycache members were harmless, but the bookkeeping rule must cover every omission regardless of file type. |
| D-028 | Later optimization cannot replace the accepted environment without passing golden-output equivalence. | Correctness and current validation come first; performance changes must remain scientifically isolated. |
| D-029 | BRIDGE autonomy/prompt governance requires no canon edit at live commit `328c7eee85cf57a5af4211b3d36f5ee7560ebc5d`. | Current generic canon already governs authority, invariants, evidence, boundaries, proportional prompts, independent reasoning, challenge, and retirement flush. MoSeq-specific practices remain project-only. |
| D-030 | Architect V5 is retired; its accumulated context contained at least one unsupported load-bearing status assertion. | A "Tier-B READY" claim and an "only one item remains" claim were both unsupported by any artifact. Retirement plus successor reconstruction is the canon-conformant response to context contamination. |
| D-031 | Tier-B has never been frozen. No formula, envelope, flag rule, receipt, or computation code for it has existed in this repository on any ref or in any local evidence root. | Exhaustive search on 2026-08-17 across all repository refs and history and all local roots returned zero occurrences. The later Architect-proposed magnitude and nonfinite-fraction battery is a proposal only and is not adopted. |
| D-032 | An OPEN_QUESTIONS status may move to CLOSED only in the same documentary change that links the exact closing primary artifact. | Status and evidence must land together, which makes in-repo narrative promotion mechanically impossible. Project-only; recorded as operating rule 40. |
| D-033 | Repository absence is not project absence, and chat assertion is not project presence. | Independent Counsel had no access to the golden host's local evidence roots. Reconciliation must cite artifacts in both directions rather than inferring from either reviewer's access boundary. |
| D-034 | The authoritative 20-UUID fitting-corpus order is the `keys` sequence of the sealed production-model artifact, identity `cb1a7b4676c3ee64ad006c26ae19f5a90a84811c7a2955d9c54321564bb046bd`. | The PCA file's `scores` group listing is name-sorted, hashes to `c2adb111d0432a470595c2a6aa09963b8898667e5e64a4a7765f7159a57a1b0d`, and is not the corpus order. Substituting it would silently reorder the corpus. |
| D-035 | D-030 correctly identified context contamination and the decision to initiate formal succession, but its wording that retirement had already completed was procedurally premature. Architect V5 remains the retiring Architect solely for the bounded successor backward pass. | Canonical BRIDGE succession transfers authority only on `PASS_HANDOFF_SUCCESSOR_STATE_RECONSTRUCTION`. D-030 is not rewritten; this decision corrects the procedural status additively. |
| D-036 | Formal Architect V5 to successor transfer completed after a two-way `PASS_HANDOFF_SUCCESSOR_STATE_RECONSTRUCTION`. Architect authority transferred completely; Architect V5 is retired and retains no substantive project authority; Owner authority is unchanged. | Succession is a governance transition and carried no scientific state transition: R1 remains unsealed, Tier-B remains unfrozen, the closure matrix remains 1 CLOSED / 2 PARTIALLY ESTABLISHED / 7 OPEN, and no row was promoted. The production-model choice remains Architect-adjudicated, not independently verified. The first post-transfer substantive operation is a bounded read-only golden-host reacquisition centered on OQ-V5-009; this decision authorizes no candidate processing. |
| D-037 | The V5 retirement reconciliation searched an incomplete evidence plane and therefore misclassified multiple completed V5 results as unresolved. The recovered primary artifacts now control. | 24 of 24 Architect-pinned V5 artifacts were found and hash-matched in a Windows Codex artifact store that durable project state never indexed. `NOT_FOUND_IN_SEARCHED_SCOPE` must not be rewritten as `PROJECT_ABSENT`; missing durable evidence is not proof that work never occurred; downstream artifacts that presuppose upstream completion are positive continuity evidence. The historical OQ-V4-006 and D-031 phrasing about 'any local evidence root' is a concrete example of overbroad negative-existence scope, and is corrected additively without disturbing D-031's still-valid conclusion that Tier-B remained unfrozen. Tier-B remains unfrozen, the eight-session roster is not rerun or replaced, OQ-V4-003 stays partial pending replacement evidence, no candidate science occurred, older immutable handoffs are untouched, and control is repaired additively. |
| D-038 | Predetermined replacements for the frozen eight-session roster are frozen under a deterministic, result-blind anchor-stratum rule; the primary roster is unchanged. Tier-B formula reacquisition is complete but Tier-B remains unfrozen. | Replacement queues derive from the accepted eligible universe by nearest rank distance to each selected anchor, ties to the earlier anchor, ordered by rank distance then StartTime then candidate id. A replacement is usable only when a primary is proven input-invalid before any validation-model result is inspected, never because of PCA/model output, syllable content, visualization, genotype or treatment behaviour, or an inconvenient result. No Tier-B formula becomes controlling because a Builder recommended it; Architect review, independent Scientific Counsel review and an Architect freeze precede any corpus-value computation. |
| D-039 | Tier-B exact formula identity is frozen before any value exists. B1 uses the frozen model-whitened 10-PC geometry, not raw persisted score geometry; B2 measures finite persisted score-row coverage. The reconstruction-error requirement is waived by documented absence of a supported implementation rather than replaced by invented methodology. | Freezing formulas before values removes the possibility of choosing a metric because its real-data values look convenient. Corpus values remain unopened, so OQ-V4-005 closes on the frozen statistic set while OQ-V4-006 stays partial pending numerical envelope construction. A breach of a future envelope is HOLD FOR ADJUDICATION and never authorises exclusion, retuning or threshold movement. R1 remains unsealed. |
| D-040 | The Tier-B numerical corpus envelopes are computed and bound: B1_lower 2.410095327226669, B1_upper 3.925567393155021, B2_lower 0.9979228486646884. OQ-V4-006 closes and every pre-seal row is now closed. | Formulas were frozen before any value existed and applied unchanged to the exact 20-session corpus roster, with zero fail-loud conditions. Extrema are empirical corpus min/max with no tolerance; degenerate-envelope semantics were frozen in advance. A future breach is HOLD FOR ADJUDICATION and never authorises exclusion, retuning or threshold movement. Corpus B1 values are in-sample with respect to the whitening the corpus estimated, a caution predeclared at freeze. R1 remains unsealed; sealing is a separate Architect action. |
| D-041 | `REAL_SESSION_PRODUCTION_VALIDATION_PROTOCOL_V4_R1` is SEALED — ARCHITECT R1, with all ten pre-seal bindings closed against cited evidence and Tier-B envelopes B1_lower 2.410095327226669, B1_upper 3.925567393155021, B2_lower 0.9979228486646884. | The seal records that every pre-result binding was frozen before any validation-candidate result existed, which is the property that makes the later run confirmatory rather than exploratory. The seal is documentary and authorizes no execution; the next scientific operation requires separate Architect authorization. Durable seal record `validation/protocols/R1_FINAL_SEAL_R1.json` SHA-256 b6d4089eeba624d446b7cdd8c99508505b36192ee809b034ee39922fe68a58aa. |
| D-042 | The controlling canonical identity of `validation/protocols/R1_FINAL_SEAL_R1.json` is SHA-256 `ac0cde7d0f65fe6e74116c7d9b4fa69764b651194ddbfb45a9394dde3e7254e2` over canonical Git object bytes, with Git blob SHA-1 `5de59c6a5446cec5ec05faf62b53e91a5a672580`. The value `b6d4089eeba624d446b7cdd8c99508505b36192ee809b034ee39922fe68a58aa` recorded in D-041 is preserved as the historical Windows CRLF-worktree representation and is NOT the controlling artifact identity. | The discrepancy is fully explained by `core.autocrlf` line-ending materialization of tracked-LF content: 3,540 canonical bytes with 57 LF newlines become 3,597 bytes under LF-to-CRLF, reproducing the historical hash bit-exactly. JSON semantics and all scientific state are unchanged. Going forward, SHA-256 bindings of repository-tracked text artifacts are defined over canonical Git object bytes at a pinned revision, never a potentially transformed working tree; a worktree-derived representation hash may be recorded only when explicitly labeled with its representation and line-ending provenance. D-041 is not altered. |
| D-043 | The R1 deterministic replay comparison contract is frozen before any primary candidate execution exists, as a closed-world `MUST_MATCH` / `DECLARED_IGNORED` partition with a qualified deterministic comparator. `OQ-V6-011` is opened and closed in this same change; `OQ-V4-008` is additively annotated and nothing in it is withdrawn. The original seal `ac0cde7d...7254e2` is cited and left byte-identical. | Protocol section 9 requires any needed equivalence criterion to be frozen before the replay result is seen. The seal bound the replay target, count and result-blind selection but not the comparison itself, and both replay-plan revisions explicitly deferred that step. Writing the contract after eight primary outputs existed would have meant choosing an exclusion list with the outputs already on disk, which is the precise failure the protocol forbids; execution was therefore held instead. Zero scientific tolerance, arrays compared as dtype plus shape plus exact C-order bytes so NaN position is bit-exact, provenance and model/PCA/config/classifier identities load-bearing, run-root paths canonicalised rather than ignored so embedded provenance stays MUST_MATCH, and only 14 enumerated non-scientific classes ignored, each justified. Anything unlisted fails. This decision authorises no candidate science and the branch is not merged. |
| D-044 | The replay comparator's C1 canonicalisation is corrected to own-root semantics, `Q19` foreign cross-run-root contamination is added, and the qualification claim is narrowed to sensitivity across every handler class plus structural failure cases. The approved scientific partition, I08 treatment, zero-tolerance exactness and OQ-V4-008 scope are unchanged; the original seal remains byte-identical. Replay comparison reports are classified PROTECTED LOCAL EVIDENCE. | Counsel's `HOLD_FABLE_COUNSEL_R1_REPLAY_COMPARISON_IMPLEMENTATION` was implementation-only. Supplying both run roots to both sides was broader than the contract text and would have masked a replay output leaking the primary root; reverting the wiring on a throwaway copy reproduced the mask (`PASS` on contaminated content) and the fix reverses it (`FAIL`). The earlier prose implied per-row perturbation of all twenty-four MUST_MATCH classes, which the suite does not do, so the claim is narrowed rather than the suite overstated. Report identity handling is recorded now because HDF5 logical names may expose candidate UUIDs: any repository-facing derivative must substitute roster-position labels, and no scientific values may be exposed. Superseded bindings are recorded, not rewritten; `fc7cacc9` is not amended. |
| D-045 | R4 is a new operator-packet identity correcting one pre-science compatibility defect: `record_r1_raw_frame_accounting.py` now accepts metadata `DepthResolution` in both the legacy string form and the JSON list form the staged sessions actually use, and fails closed through the accounting receipt for every other representation. R3 is preserved as historical and is not rewritten; the failed slot-01 output is preserved and must never be reused. The replay contract's operator provenance is rebound to R4 commit `f026298a0cf2a0fa97bedb29245424bddcf0713b` with the scientific partition byte-identical. | The R3 operator aborted all eight primaries before science with RAW_FRAME_ACCOUNTING_FAILED because a non-empty list is truthy and `resolution or ""` fed a list to `re.match`. The condition was systemic across all eight slots. R3's synthetic fixture used the string form so its gate never exercised the list form, and Tier-E failed one step earlier at the provenance preflight, so neither could have caught it; the R4 gate fixture is switched to the list form precisely so the representation that defeated R3 now traverses raw-frame accounting. Fixing only the crash would not have sufficed: the helper's fallback returns `frames: None`, which fails the timestamp-row check and yields a HOLD anyway, so list-form support was the actual requirement. Scientific settings, thresholds, roster, replacements, PCA, model, config, classifier, replay comparison semantics and tolerances are all unchanged, and no raw data was altered. **Additive annotation (D-046):** the R4 qualification report carried one inverted documentary field, corrected without touching operator or scientific material; the packet manifest and contract packet-manifest binding moved accordingly. |
| D-046 | The R4 qualification report's `full_operator_gate.candidate_recording_processed` is corrected from `true` to `false`, and the R4 packet manifest and the replay contract's packet-manifest binding are regenerated to match. This is a documentary evidence correction only. | The gate test emits `candidate_recording_processed=false` and the captured gate output records the same, so the report contradicted both of its own sources. The cause was a generator expression that recorded whether the assertion line was present rather than the value it asserted, inverting the sense of a field whose whole purpose is to attest that no candidate recording was processed - precisely the claim that must not be wrong. Nothing executable changed: the helper, gate logic, comparator code, MUST_MATCH and DECLARED_IGNORED partition, scientific settings and raw data are all untouched, the comparator qualification rerun is unchanged at 19/19, D-045 is annotated rather than rewritten, and the already-pushed commits are not amended. |
| D-047 | The runtime PCA binding is corrected from the frozen training-score artifact `pca_scores.h5` (`26e30500...`) to the PCA component basis `pca.h5` (`6b587854...`), and one semantic role gate is added requiring the runtime PCA artifact to contain `/components` with shape (25, 6400) and dtype float32. Fresh R5 run specs are derived from the R4 specs with the PCA binding as the only change; the R4 specs are not mutated. The training-score artifact is preserved, not redefined. | `moseq2-pca apply-pca` reads `/components`, which the score artifact does not contain, so R4 slot 01 aborted at stage 03 after ROI and full extraction had already run - the first candidate science in the programme, preserved unaltered. A hash-only gate could not catch this because the score artifact hashed exactly as bound, which is why the correction adds a role check rather than another checksum. Component lineage was established under `PASS_ARCHITECT_PCA_COMPONENT_LINEAGE_ESTABLISHED_R1`, where regeneration from `pca.h5` reproduced the frozen scores to within one float32 ULP with identical shapes, dtypes, NaN placement and byte-identical `scores_idx`; that residual is accepted as corroborative lineage evidence only and does not relax the zero-tolerance primary/replay comparison contract. This is a provenance role correction, not a PCA refit, model refit, retuning, new PCA selection, or any change to Tier-B/Tier-C criteria; raw inputs, roster, config, classifier, production model, thresholds and replacements are unchanged, and R3/R4 packets and every failed run directory are preserved. |
| D-048 | The companion `pca.yaml` (`ba47df9b...`, 2714 bytes) is bound in the R5 run specs as `scientific_artifacts.pca_yaml`, verified in the provenance preflight, and stage 03 now invokes apply-pca with the verified original PCA path rather than the run-local copy. The PCA component basis is unchanged. | `moseq2-pca` derives the companion as `splitext(pca_file)[0] + '.yaml'` and `get_pca_yaml_data` raises `OSError` when it is absent, so passing the run-local copy `inputs/pca_components.h5` without writing a sibling yaml would have cleared the `/components` role gate and then failed anyway. The companion carries the frozen filter and mask parameters actually consumed at apply time, which makes it a consumed scientific dependency rather than incidental metadata, so it is bound and hashed rather than merely allowed to exist. Copying a yaml into the run tree was rejected because that would manufacture a second copy of a frozen scientific input; using the verified original keeps one authoritative artifact while the immutable evidence copy of the basis is still recorded. Qualification is 22/22 including live proof that a wrong or missing companion fails closed at exit 42 / FAILED_HOLD. No candidate science, no fit route, and the replay partition, tolerance, C1 and comparator code are untouched. |
| D-049 | `plane_ransac` is made deterministic with a local `np.random.RandomState(0)` on moseq2-extract `2c9cd86571bcc23ad6870e4da344e0f558f3f54c`, the output policy `roi_plane_fit` is bumped to `lstsq-inlier-refit-deterministic-rng0`, and the replay comparator is corrected for generated-extraction-UUID canonicalisation, joblib model loading, decoded-TIFF comparison and diagnostic presence-only. | The R5 replay failed on real data and the decomposition separated three causes: unseeded RANSAC sampling was genuine nondeterminism that propagated from ROI through extraction, scalars and PCA scores into the applied syllable labels, while the unclassified files, the unreadable model container and the TIFF byte comparison were defects in my own contract and comparator that were masking the signal. Fixing the latter is not a relaxation: the corrected contract is stricter, and re-running it against the preserved R5 pair still FAILS with 73 differences and now zero unclassified, missing, extra or unreadable units, which isolates the nondeterminism cleanly. The seed choice is arbitrary but must be fixed and recorded; nothing else in RANSAC changed, so a post-fix ROI is one of the same population the unseeded code could have produced. No PCA refit, no ARHMM refit, no kappa rerun, and the roster, config, classifier, PCA basis and companion, production model and Tier-B/Tier-C criteria are all carried forward unchanged. R5 runs are preserved as the evidence that motivated this. |
| D-050 | `PASS_ARCHITECT_R1_R6_SCIENTIFIC_COUNSEL_ACCEPTED_R1`. Scientific Counsel disposition `PASS_FABLE_COUNSEL_R1_R6_READY_FOR_VERIFICATION` is accepted. Claim boundary, recorded verbatim: "The extraction RNG policy differs between the training-corpus era and R6, but the underlying RANSAC estimator and scientific parameters are unchanged. The frozen training corpus remains an archival realization of that estimator and is not required to be deterministically regenerated. No PCA refit, ARHMM refit, or kappa rerun is required for the sealed R1 held-out validation. Compatibility of R6-held-out extraction with the frozen PCA/model is tested by the already-frozen Tier-B/Tier-C and replay framework rather than asserted from regeneration identity." Deterministic re-extraction of the training corpus would constitute a new instrument and is outside R1. The R5 candidate outputs remain superseded validation evidence and are preserved. The next gate is Independent Verifier review. Candidate execution remains unauthorized. | The R6 determinism fix changes how a hypothesis is *sampled*, not what is estimated: iteration count, noise tolerance, inlier ratio, scoring, tie-break, the least-squares inlier refit and ROI ranking are untouched, so a post-fix ROI is drawn from the same population the unseeded code could have produced. That is why the frozen training corpus does not need regenerating: it is one archival realization of an unchanged estimator, and demanding bit-identical regeneration would confuse reproducibility of a *record* with validity of an *instrument*. Re-extracting the corpus deterministically would instead create a different instrument and force a PCA refit, an ARHMM refit and a kappa rerun - precisely the scope R1 excludes. Compatibility of R6 held-out extraction with the frozen PCA basis and production model is therefore left to the pre-frozen Tier-B/Tier-C envelopes and the replay contract, which were fixed before any candidate result existed and can fail; asserting compatibility from regeneration identity would be an untested claim substituted for a test that already exists. This entry records an acceptance and a scope boundary only: no code, comparator, contract, packet, run spec, source, seal or qualification artifact is changed by it, and it authorizes no candidate processing. |
| D-051 | The replay comparator implementation is corrected for three representation defects found after the R6 replay: wall-clock pipeline-provenance `written` fields are declared ignored (I16-I19) while the surrounding provenance records are compared field by field, the embedded model object is compared as a presence marker rather than a `repr` digest, and fixed-capacity HDF5 `S<N>` itemsize is removed from the string equality predicate after C1/C2 canonicalisation. Scientific tolerance stays 0 and numeric dtype, shape and exact bytes stay mandatory. | The R6 replay ran clean - all eight primaries and the replay exited 0 - and an identity-redacted, value-blind decomposition established that every scientific array and every applied label array was already bit-exact. The seven residual comparator failures were therefore not science: two pipeline writers stamp `datetime.datetime.now()` into provenance, the embedded ARHMM's `repr` carries a heap address that changes on every load, and HDF5 fixed-width string capacity tracks the un-canonicalised run-root path length so C1-identical text still differed by dtype. Each correction narrows rather than relaxes: provenance is now compared field by field instead of as an opaque blob, the model key must still be present, and a differing canonical path text still fails. Qualification is 37/37 including negative controls proving that any non-`written` provenance field, an acquisition timestamp, a missing model key, a parameter perturbation, a differing canonical path text and a numeric dtype mismatch all still FAIL. The corrected comparator has deliberately NOT been run against the preserved R6 or R5 outputs, so no rule was tuned against a known result. |
| D-052 | The comparator-correction verification gate is **CLOSED** on correction tip `1cd7c900780a423f2b2186025ee3a324b2bf7fbd` (parent `54d8d7b5783c0810088ed96694a6bc10dcd7c94f`) under Independent Verifier disposition `PASS_FABLE_VERIFIER_R1_R6_COMPARATOR_CORRECTION`, with 37/37 qualification independently reproduced at exit 0. The corrected comparator has NOT been run against the nine preserved R6 runs. The next authorized scientific operation is a comparison-only evaluation of the verified corrected comparator against those preserved outputs, emitting a new separately named evidence artifact that must not overwrite or mutate `REPLAY_COMPARISON_REPORT_R6.json`, followed by a STOP for Architect adjudication before any seal advancement or candidate work. Candidate processing remains prohibited. | This is a checkpoint, not a scientific step: it records that a verification gate closed and fixes what the next operation may and may not do, so that a successor cannot mistake a verified comparator for a completed comparison. Separating the two matters here because the corrected comparator was deliberately never run against a known result during its own construction; running it now is the first moment its verdict on real preserved evidence becomes visible, and that verdict must be produced under a contract that was frozen and verified beforehand. Requiring a separately named artifact preserves the original R6 report as the record of what the pre-correction comparator said, so the effect of the correction stays auditable rather than being overwritten by its own successor. The original seal `ac0cde7d0f65fe6e74116c7d9b4fa69764b651194ddbfb45a9394dde3e7254e2`, the R6 packet `3bfc5ac04dd71f1d7b7a6010442561e2ef3d6399c88ebff88c4390781a59de5e` at 75/75, the original R6 report `22be3cecc3c586a8f47950802be71fcc689741eeb957340d0d1a94954d288a2c` and all nine preserved run directories are untouched by this entry, which changes documentation only. |
| D-053 | `PASS_ARCHITECT_R1_R6_SCIENTIFIC_DETERMINISM_ESTABLISHED_COMPARATOR_REPAIR_REQUIRED_R1`. Claim class ARCHITECT_ADJUDICATED. The preserved R6 scientific arrays and final applied labels are bit-exact; the seven original residual comparator failures were representation/comparator-implementation effects; the comparator repair is **BUILT** and **INDEPENDENTLY VERIFIED** (`PASS_FABLE_VERIFIER_R1_R6_COMPARATOR_CORRECTION`); the corrected comparator has **not** yet been evaluated against the preserved R6 real outputs; overall R6 execution acceptance therefore remains **HOLD**; no candidate rerun is required; scientific tolerance remains zero. Bound evidence: receipt `/home/ajm/moseq2-validation-20260730/evidence/r1_primary_execution_r6_r1/R6_RESIDUAL_DECOMPOSITION_RECEIPT.json` SHA-256 `4b2d931b5ea1f44ed818cd5aaae6b3a354421cdd3be9c7d44b0f081e2a241675`; original R6 comparator report SHA-256 `22be3cecc3c586a8f47950802be71fcc689741eeb957340d0d1a94954d288a2c`; access limitation: protected golden-host local evidence, not repository-accessible. D-051 and D-052 are not rewritten. | This entry exists so a successor inherits the acceptance and its boundary together. The determinism claim and the execution-acceptance claim are separate, and only the first is accepted: the instrument is shown to repeat itself bit-for-bit, while the measuring device that reads it has been repaired and independently verified but never yet pointed at the preserved evidence. Binding the local receipt by hash keeps the continuation-critical decomposition out of chat-only memory, and recording the access limitation prevents a later reader from assuming it was repository-verified. Zero tolerance is restated because the repair narrows what is compared rather than widening what is forgiven. |
| D-054 | The already-completed Independent Verifier report underlying `PASS_FABLE_VERIFIER_R1_R6_COMPARATOR_CORRECTION` is now durably preserved. Artifact path `ai/reviews/FABLE_VERIFIER_2026-08-18_R1_R6_COMPARATOR_CORRECTION.md`, exact SHA-256 `2c5fac827229b5be642c5196107540d1bdf1c6a1f4ec901ef021397e6b8cc57c`. Claim class INDEPENDENT_VERIFIER_REPORT / DURABLY_RECORDED. Verified target `1cd7c900780a423f2b2186025ee3a324b2bf7fbd`. Disposition `PASS_FABLE_VERIFIER_R1_R6_COMPARATOR_CORRECTION`. The report records an independent 37/37 reproduction of the qualification suite at exit 0. This entry is **evidence-link recovery only, not a new verification**, and this Builder operation does not independently re-verify Fable's report. No comparator, contract, qualification artifact, preserved R6 output, scientific artifact or seal changed. The prior succession classification `COMPLETED_WORK_EVIDENCE_LINK_MISSING` for this result is **RESOLVED** by this exact durable artifact binding. D-051, D-052 and D-053 are not rewritten. | BRIDGE Rule 23 requires an accepted continuation-critical result to be durably reacquirable by exact artifact identity and stable hash, and Rule 24 item 3 requires completed work with a missing evidence link to be serialized and bound before retirement rather than carried in chat. The verification had genuinely occurred and was described accurately in the control plane, but its report existed only as conversation, so a successor could read the disposition without ever reaching the reasoning behind it. Preserving the report verbatim and binding its hash closes that gap without reopening the review: the disposition, the verified target and the 37/37 result are recorded as the verifier stated them, not re-derived. Nothing here authorizes dependent work; overall R6 execution acceptance remains HOLD and the corrected comparator remains unevaluated against preserved R6 outputs. |
| D-055 | `PASS_ARCHITECT_R1_R6_CORRECTED_COMPARATOR_EVALUATION_R1`. The independently verified corrected replay comparator `0697df76e49d43b3bf352d41230cd2385e3d5f8bdc87ddaf9eeaeee53c588840` was applied once, comparison-only, to the preserved R6 slot-01 primary/replay pair and returned exit `0`, `disposition` PASS, `failure_count` 0, `status_counts` {"IGNORED": 26, "MATCH": 1230}, `must_match_units` 1230, `declared_ignored_units` 26, `unclassified_units` 0, runtime `contract_sha256` `b5e2dcb3c0179ce1dba8bbb499a7335096c570cf1fbb091e0c6e4e00c2128d13`. Claim class BUILDER_PRODUCED_EVIDENCE / ARCHITECT_ADJUDICATED / LOCAL_EVIDENCE_BOUND. Evidence artifact `/home/ajm/moseq2-validation-20260730/evidence/r1_primary_execution_r6_r1/REPLAY_COMPARISON_REPORT_R6_CORRECTED_COMPARATOR_R1.json` SHA-256 `c3ffdad3bd7ceb6d5b4e1fb3ebe001b233b12a0319df559da23314a77a3b9783`; access limitation: protected golden-host local evidence, not repository-accessible. This **closes the R6 execution/replay acceptance subgate that D-053 left on HOLD**: the preserved R6 primary/replay outputs satisfy the frozen corrected comparison contract with zero MUST_MATCH failures, and **no candidate rerun is required**. The historical `REPLAY_COMPARISON_REPORT_R6.json` remained `22be3cecc3c586a8f47950802be71fcc689741eeb957340d0d1a94954d288a2c` and scientific tolerance remained 0. This is **not** `PASS_REAL_SESSION_PRODUCTION_VALIDATION`; final production validation still requires satisfaction or adjudication of the remaining sealed-protocol criteria, including Tier-D qualitative review. No model selection, PCA, ARHMM, extraction parameter, comparator semantics, tolerance or sealed scientific criterion is reopened. Execution record: an initial runner-wrapper launch terminated before producing a report, captured output or exit code, with no substantive mutation and no observed verdict; the verdict-producing invocation then occurred once with the accepted bytes and arguments; its stdout and stderr were not retained because temporary scratch capture was lost. That is an ancillary evidence-capture defect, not a comparator or scientific defect, and no rerun is authorized or required to recover console output. D-051, D-052, D-053 and D-054 are not rewritten. | D-053 accepted R6 scientific determinism but deliberately held overall execution acceptance open, because the repaired measuring device had never been pointed at the preserved evidence. This entry records the first and only time it was, under a contract frozen and independently verified beforehand, so the verdict cannot have been tuned to it: comparator, contract and qualification bytes were fixed at `1cd7c900780a423f2b2186025ee3a324b2bf7fbd` before the run and re-hashed unchanged after it, and the runtime `contract_sha256` recorded by the comparator itself confirms it read the accepted contract rather than a substitute. Zero MUST_MATCH failures at tolerance 0 is what the correction predicted without ever having been tested against this pair, which is why the result closes the subgate rather than merely reporting a number. The original report is preserved untouched so the effect of the correction remains auditable against what the pre-correction comparator said. The boundary is restated because a clean replay comparison shows the instrument repeats itself under the frozen contract; it does not establish the sealed protocol remaining criteria, and collapsing those two claims is the specific error this entry exists to prevent. The console-output loss is recorded rather than repaired because the substantive artifact is durably hash-bound, and rerunning a completed exit-0 result to recover a log line would risk more than it recovers. |
