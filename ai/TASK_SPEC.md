# Current Task Specification

Date: 2026-08-13

## Operation

`REAL_SESSION_PRODUCTION_VALIDATION_R1_PRESEAL_REACQUISITION`

Status: **NOT YET AUTHORIZED FOR EXECUTION**. This file defines the next bounded
scientific operation; it does not itself authorize it.

## Objective

Reacquire and freeze, read-only, the eight pre-result inputs required to seal
`REAL_SESSION_PRODUCTION_VALIDATION_PROTOCOL_V4_R1` without opening any
production-model validation output.

## Required outputs

1. exact locked 20-session fitting-corpus UUID roster;
2. metadata-only eligible outside-corpus inventory with rig, animal, date, raw
   QC, and prior processing/result-inspection history;
3. frozen validation roster, predetermined replacements, modeled/inspected
   exceptions, and expected-flag dimensions for deliberate challenges;
4. authoritative repaired visualization/app/source identity plus a passing
   180-degree-orientation regression or a narrow HOLD;
5. exact supported PCA/model global QC statistics;
6. corpus-only Tier-B/Tier-C envelopes and numeric mechanical flag rules;
7. exact Tier-E negative-control condition;
8. deterministic replay plan, including whether any materially distinct path
   justifies more than one replay.

## Accepted inputs

Use `ai/CURRENT_STATE.md`, `ai/HANDOFF_CURRENT.md`,
`validation/protocols/REAL_SESSION_PRODUCTION_VALIDATION_PROTOCOL_V4_R1.md`,
and the immutable identities they cite.

## Authority boundary

The operation is read-only except for a fresh pre-seal evidence directory and
durable project-control records. It may inspect metadata, raw-input integrity,
locked-corpus baseline data, source, environment, and pre-existing
visualization-regression evidence needed for the eight bindings.

## Prohibited

No fitting, PCA refit, model application to validation sessions, crowd-movie or
syllable-result inspection, validation PCA/model diagnostic inspection,
genotype/treatment analysis, biological interpretation, performance work,
environment modernization, source remediation, or threshold tuning after a
validation value is seen.

## Stop conditions

Hold rather than guess if an accepted identity conflicts, a roster or exposure
history cannot be reconstructed honestly, visualization qualification cannot
be established, a required metric has no supported implementation, or a numeric
rule would require opening validation values first.

## End boundary

Return the frozen pre-seal package for Architect adjudication and stop. Do not
seal or execute real-session validation autonomously.
