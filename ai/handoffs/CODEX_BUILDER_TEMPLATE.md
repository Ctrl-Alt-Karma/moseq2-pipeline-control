# Codex Builder Handoff

## Role boundary

Codex is the primary builder and executor. Do not broaden scope without first updating `ai/TASK_SPEC.md`. Do not merge, delete audit branches, add raw recordings, or treat a PR description as evidence.

## Assignment

- Task ID:
- Repository:
- Remote URL:
- Base branch and exact SHA:
- Candidate branch and exact SHA:
- In-scope paths:
- Out-of-scope paths:
- Finding IDs:

## Required work

- Exact source claim to test:
- Falsifying test and why it would fail at the buggy base:
- Implementation constraint:
- Pinned environment/profile:
- Exact commands:

## Evidence to return

- Repository and exact resulting SHA:
- Paths and line-level behavior changed:
- Red result at exact base SHA:
- Green result at exact candidate SHA:
- Full command and result:
- Working-tree cleanliness:
- Unresolved limitations:
- Draft PR URL and state:

Stop and update `TASK_SPEC.md` before any scope expansion.
